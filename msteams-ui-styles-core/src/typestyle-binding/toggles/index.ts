import { style } from 'typestyle';
import { Context } from '../../raw/context';
import * as toggleRaw from '../../raw/toggles';
/*
function label(c: Context) {
  return style(toggleRaw.label(c));
}

function input(c: Context) {
  return style(toggleRaw.input(c));
}

function slider(c: Context) {
  return style(toggleRaw.slider(c), {
    $nest: {
      '&:before': toggleRaw.sliderBefore(c),
      '&:focus:after': toggleRaw.sliderFocus(c),
    },
  });
}

function sliderChecked(c: Context) {
  return style(toggleRaw.sliderChecked(c), {
    $nest: {
      '&:before': toggleRaw.sliderBeforeChecked(c),
      '&:focus:after': toggleRaw.sliderFocusChecked(c),
    },
  });
}

export const toggles = {
  label,
  input,
  slider,
  sliderChecked,
};
*/
