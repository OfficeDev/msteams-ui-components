export interface FontSizePalate {
  title: {
    fontSize: string;
  };
  title2: {
    fontSize: string;
  };
  base: {
    fontSize: string;
  };
  caption: {
    fontSize: string;
  };
  xsmall: {
    fontSize: string;
  };
}
