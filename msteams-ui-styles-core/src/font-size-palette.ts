export interface IFontSizePalette {
  title: {
    fontSize: string;
    lineHeight: string;
  };
  title2: {
    fontSize: string;
    lineHeight: string;
  };
  base: {
    fontSize: string;
    lineHeight: string;
  };
  caption: {
    fontSize: string;
    lineHeight: string;
  };
  xsmall: {
    fontSize: string;
    lineHeight: string;
  };
}
