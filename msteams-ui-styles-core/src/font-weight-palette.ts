export interface IFontWeightPalette {
  semilight: {
    fontFamily: string;
    fontWeight: number;
  };
  regular: {
    fontFamily: string;
    fontWeight: number;
  };
  semibold: {
    fontFamily: string;
    fontWeight: number;
  };
  bold: {
    fontFamily: string;
    fontWeight: number;
  };
}
